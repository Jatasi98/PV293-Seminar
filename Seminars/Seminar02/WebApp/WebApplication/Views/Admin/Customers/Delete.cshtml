@model WebApplication1.Entities.Customer
@{
    Layout = "_Layout";
    ViewData["Title"] = "Delete Customer";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb mb-0">
            <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Customers" asp-action="Index">Customers</a></li>
            <li class="breadcrumb-item active" aria-current="page">Delete</li>
        </ol>
    </nav>

    <div class="card shadow-sm border-danger">
        <div class="card-header bg-danger-subtle">
            <h1 class="h5 mb-0 text-danger">Delete Customer</h1>
        </div>
        <div class="card-body">
            <p class="mb-3">Are you sure you want to delete this customer?</p>

            <dl class="row">
                <dt class="col-sm-3 text-muted">Name</dt>
                <dd class="col-sm-9 fw-semibold">@Model.FirstName @Model.LastName</dd>

                <dt class="col-sm-3 text-muted">Email</dt>
                <dd class="col-sm-9">@Model.Email</dd>
            </dl>

            <form asp-area="Admin" asp-controller="Customers" asp-action="Delete" method="post" class="mt-3">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Delete
                    </button>
                    <a class="btn btn-outline-secondary"
                       asp-area="Admin" asp-controller="Customers" asp-action="Index">
                        Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
