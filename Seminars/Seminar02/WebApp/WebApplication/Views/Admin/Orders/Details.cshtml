@model WebApplication1.Entities.Order
@{
    Layout = "_Layout";
    ViewData["Title"] = "Order Details";
}

<div class="container">
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb mb-0">
            <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Orders" asp-action="Index">Orders</a></li>
            <li class="breadcrumb-item active" aria-current="page">Details</li>
        </ol>
    </nav>

    <div class="card shadow-sm">
        <div class="card-header"><h1 class="h5 mb-0">Order Details</h1></div>
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3 text-muted">Order #</dt>
                <dd class="col-sm-9 fw-semibold">@Model.Id</dd>

                <dt class="col-sm-3 text-muted">Customer</dt>
                <dd class="col-sm-9">@((Model.Customer != null) ? Model.Customer.Email : Model.CustomerId.ToString())</dd>

                <dt class="col-sm-3 text-muted">Order Date</dt>
                <dd class="col-sm-9">@Model.CreatedOnUTC.ToLocalTime()</dd>

                <dt class="col-sm-3 text-muted">Total</dt>
                <dd class="col-sm-9">@Model.Total.ToString("C")</dd>
            </dl>
        </div>
        <div class="card-footer d-flex gap-2">
            <a class="btn btn-outline-primary"
               asp-area="Admin" asp-controller="Orders" asp-action="Edit" asp-route-id="@Model.Id">
                <i class="bi bi-pencil"></i> Edit
            </a>
            <a class="btn btn-outline-secondary"
               asp-area="Admin" asp-controller="Orders" asp-action="Index">
                Back to list
            </a>
        </div>
    </div>
</div>
